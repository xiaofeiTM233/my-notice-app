<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinNotify.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="10" Padding="20,18,20,6">
            <TextBlock Text="通知中心" Style="{StaticResource SubtitleTextBlockStyle}" />
            <Border x:Name="UnreadBadge" Background="#FF0A84FF" CornerRadius="9" Padding="8,2" VerticalAlignment="Center">
                <TextBlock x:Name="UnreadText" Foreground="White" FontSize="12" />
            </Border>
        </StackPanel>

        <Grid Grid.Row="1" Padding="20,6,20,8" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <AutoSuggestBox x:Name="SearchBox" PlaceholderText="搜索标题或内容" />
            <ComboBox x:Name="CatBox" Grid.Column="1" MinWidth="110" />
        </Grid>

        <ListView x:Name="List" Grid.Row="2" Padding="14,4,14,4" SelectionMode="None" IsItemClickEnabled="True" />

        <Border Grid.Row="3" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" Padding="16,10">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                <Button x:Name="BtnAllRead" Content="全部已读" />
                <Button x:Name="BtnClear" Content="清空全部" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
